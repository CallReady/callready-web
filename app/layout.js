export const metadata = {
  title: "CallReady",
  description: "Practice real phone calls with AI"
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
